<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".SmolChatApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmolLMAndroid"
        android:localeConfig="@xml/locales_config"
        tools:targetApi="31">

        <!-- Single launcher activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.SmolLMAndroid">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Share-to-chat entry point -->
        <activity
            android:label="💬 LLM"
            android:name=".ui.screens.chat.ChatActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <!-- Other internal screens -->
        <activity android:name=".ui.screens.model_download.DownloadModelActivity" />
        <activity android:name=".ui.screens.manage_tasks.ManageTasksActivity" />

        <!-- Model selection screen (opened from inside the app) -->
        <activity
            android:name="io.shubham0204.smollmandroid.ui.ModelSelectionActivity"
            android:exported="false" />
    </application>

</manifest>